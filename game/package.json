{
  "name": "@otterblade/game",
  "version": "0.1.0",
  "description": "Otterblade Odyssey - Vanilla JS + Matter.js Game",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8000 --directory src",
    "build": "esbuild src/main.js --bundle --outfile=dist/main.js --format=esm --minify"
  },
  "dependencies": {
    "matter-js": "^0.20.0",
    "howler": "^2.2.4",
    "yuka": "^0.9.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
